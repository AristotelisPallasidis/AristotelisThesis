<UserControl x:Class="AristotelisThesis.WPF.Controls.WeeklyAttendanceGraph"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AristotelisThesis.WPF.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="#E6E6E6"
            Padding="12"
            CornerRadius="8 8 8 8">

            <Grid Grid.Row="0" Margin="6 6 0 0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>


                <Grid Grid.Row="0">
                    <TextBlock Text="Γραφημα εβδομαδιαίας παρακολουθησης" FontWeight="Medium" FontSize="16"></TextBlock>
                </Grid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 18 0 0" VerticalAlignment="Center">
                    <TextBlock
                        x:Name="PercentText"
                        Text="—"
                        Foreground="{StaticResource ColorDanger}"
                        FontSize="64"
                        FontWeight="Black"
                        HorizontalAlignment="Left">
                    </TextBlock>
                    <TextBlock
                        Text="παρακολούθηση"
                        VerticalAlignment="Bottom"
                        Margin="12 0 0 16"
                        FontSize="14"
                        Foreground="#666">
                    </TextBlock>
                </StackPanel>

                <!-- Bar chart: one bar per day of the current week -->
                <Grid Grid.Row="2" Margin="0 24 0 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>

                    <Grid x:Name="BarsGrid" Grid.Row="0" SizeChanged="BarsGrid_SizeChanged" />
                    <Grid x:Name="LabelsGrid" Grid.Row="1" Margin="0 6 0 0" />
                </Grid>

            </Grid>

        </Border>

    </Grid>

</UserControl>
